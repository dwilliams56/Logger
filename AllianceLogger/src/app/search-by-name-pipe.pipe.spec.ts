import { SearchByNamePipePipe } from './search-by-name-pipe.pipe';

describe('SearchByNamePipePipe', () => {
  it('create an instance', () => {
    const pipe = new SearchByNamePipePipe();
    expect(pipe).toBeTruthy();
  });
});
